# Admin Dashboard Guide

MACS 프로젝트는 운영 편의성을 위해 **Streamlit** 기반의 Admin Dashboard를 제공합니다.
이 대시보드를 통해 터미널 접속 없이도 로컬에 저장된 토론 내역과 Vector DB(Chroma)의 상태를 확인할 수 있습니다.

## 1. 실행 방법 (How to Run)

프로젝트 루트 디렉토리에서 아래 명령어를 실행합니다.

```bash
uv run streamlit run app/admin/dashboard.py
```

명령어 실행 후 자동으로 브라우저가 열리며 `http://localhost:8501`로 접속됩니다.

## 2. 기능 소개 (Features)

### 📂 Tab 1: File Storage
`data/debates/` 디렉토리에 저장된 **Markdown 파일(.md)**들을 조회합니다.
- **File List**: 저장된 파일 목록을 최신순으로 보여줍니다.
- **Preview**: 선택한 파일의 렌더링 된 결과(Markdown)와 원본 소스(Raw)를 비교해서 볼 수 있습니다.

### 💾 Tab 2: Vector DB (Chroma)
`data/chroma_db` 경로의 **ChromaDB** 상태를 검사합니다.
- **Collection**: 현재 생성된 컬렉션 목록을 보여줍니다. (기본: `debates`)
- **Count**: 저장된 전체 문서(Document) 개수를 확인합니다.
- **Peek**: 최근 추가된 5개의 아이템(Metadata + Content)을 미리봅니다.

### 🔍 Tab 3: Search Playground
**Semantic Search(의미 기반 검색)**를 테스트할 수 있는 공간입니다.
- **Query Input**: 검색어를 입력하면 Embedding 후 가장 유사한 문서 3개를 찾아줍니다.
- **Distance**: 검색어와의 거리를 보여줍니다 (낮을수록 유사함).
